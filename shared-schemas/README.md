# shared-schemas

This directory centralizes JSON Schema definitions shared by runtime, experiments, and data pipelines. Use versioned schemas here to enforce consistent interfaces across sub-repositories.
