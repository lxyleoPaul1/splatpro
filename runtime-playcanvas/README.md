# runtime-playcanvas

This directory hosts PlayCanvas runtime code for scene rendering, interaction logic, and platform integration. It is initialized as an empty placeholder so you can copy in existing runtime assets safely.
